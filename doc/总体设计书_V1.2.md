《"花伴"(FloraMate) 智能家庭灌溉系统》总体设计书 V1.2

────────────────────────────────────────

0. 修订记录 (Revision History)

V1.0 → V1.1 关键变更：
  (1) 联网架构：明确为"米家智能插座外部托管定时"的极简方案，正式形成"L0 米家插座 / L1 水泵总电源 / L2 路阀"三级断电架构（V1.0 硬件落地：**CH1=水泵总电源，CH2~CH5=电磁阀**）。
  (2) 水位检测：新增独立水位检测子系统，采用"超声波连续测量 + 浮球硬件兜底"双保险方案。
  (3) 浇灌策略：明确"单路依次执行 + PWM 阶梯调速 + 喷射半径覆盖"的执行逻辑。
  (4) 上电时序：新增浪涌抑制、自检流程、配置存储、独立看门狗、执行超时硬保护。
  (5) 设计哲学：新增"原则四：托管式智能（Hosted Intelligence）"。
  (6) 主控选型理由补充：F401RCT6 当前国内市场现货价低于 F103C8T6，性能与成本同时占优。

V1.1 → V1.2 关键变更：
  (a) AWG 工艺勘误：明确 AWG 数字越大线径越细；20AWG 主干承载 12V/4A 余量 ~87%，为合理工程选择，原方案保留。
  (b) 水位传感器选型：在 §3.4 增加 5 型号横向对比表与推荐理由，最终采用"AJ-SR04M 超声波 + PSF-202 浮球开关"双保险组合。
  (c) 浇灌策略澄清：地插喷头的"扫描覆盖"机理写入正式文本——通过 PWM 阶梯改变出水压力，使机械喷头射程由近至远连续变化，单路即覆盖近-中-远三段半径。
  (d) 米家插座方案的"上电唤醒/任务结束/等待断电"完整时序写入 §5 软件状态机。
  (e) 任务结束后 MCU 主动进入 STOP 模式，但不主动通知米家插座；改为依赖米家定时窗口结束后自动断电（简化外部协议）。

V1.2 执行层固态化更新（与 `硬件设计方案_V3.0.md` 对齐）：
  (f) **取消机械继电器与 5V_Dirty 线圈网（LM2596）**：阀与水泵改由 **TLP281-4 光耦 + AO4407A P 沟道 MOSFET 高边开关**驱动，高电平有效（GPIO High = 负载 ON），GPIO 复位为 Low 即全部截止（失效安全）。
  (g) **取消独立"水泵总电源"通道（原 CH1）**：水泵为第 6 路 P-MOS 高边，由 PA0 (TIM2_CH1) 直接 PWM 调速；"泵不空转"由**软件干转互锁**（至少一路阀已开）保证。
  (h) 阀路扩展为 **Z1~Z5（PA1~PA5）**；按键移至 PA6/PA7/PB0/PB1；配置存储改用外置 **AT24C08C**（1 KB）。
  (i) 水泵 PWM 载波由 25 kHz 调整为 **100 Hz**（当前调速参数）。

────────────────────────────────────────

1. 项目愿景与命名释义 (Project Vision)

项目代号：花伴 (FloraMate)
  · Flora：植物的拉丁词根，承载"生命与陪伴"语义；
  · Mate：伙伴。寓意"陪伴每一株植物"。

愿景定位：打造一台在用户离家或忙碌时，能够像管家一样提供精准、安静、极度可靠的居家植物自动灌溉伴侣。

定位修订（V1.1 起）：本机走"减法智能"路线 —— 把"何时浇"交给米家生态外部托管，把"如何浇好"由本机专注完成。本机不联网、无 RTC、无电池备份；每次上电即视为一次合法的浇灌指令，执行完毕主动请求结束，等待外部插座断电下一次唤醒。

核心矛盾解决：打破传统家用浇花器"容易死机、寿命短、常漏水"的行业痛点，将工业控制与汽车电子领域的"高可靠性设计"降维应用到家庭场景中。

────────────────────────────────────────

2. 总体设计哲学 (Overall Design Philosophy)

"花伴"系统在总体思路上放弃廉价的"单板高集成度"方案，转而采用分布式、高冗余的模块化架构。核心价值观由 V1.0 三大原则扩展为四大原则：

原则一：绝对的楚河汉界（强弱电隔离）
家用的水泵和电磁阀是极强的干扰源（感性负载）。本设计在物理上将系统彻底劈为两半：左边是"12V/5V 脏电动力区"，右边是"3.3V/5V 纯净控制区"。两区之间的能量与信号仅通过"磁（隔离变压器）"与"光（光耦）"传递，地线（GND）在物理世界中永不相交。

原则二：悲观主义的安全底线（Fail-Safe 故障兜底）
系统假设元器件一定会坏。通过失效安全的拓扑设计，即使发生水泵驱动管热击穿短路、MCU 程序跑飞等极端故障，系统仍可通过"GPIO 复位即 P-MOS 截止"的默认安全态、米家插座物理总断电以及浮球开关，强行切断水源，杜绝"水漫金山"。

原则三：精细的能量分配（线束分级）
放弃"一根细线走天下"的创客思维，采用工业线束的变径管理。动力主干道提供极低阻抗的澎湃动力，控制末梢则采用柔性连接，兼顾电气性能与物理接口的抗应力寿命。

原则四：托管式智能（Hosted Intelligence，V1.1 新增）
本机不内建联网与时钟。"何时浇水"由米家智能插座定时通断 12V 适配器一次性完成；"如何浇水"由本机一次性闭环执行。任务结束即主动请求外部断电，整机进入零功耗等待。系统每天在"完全断电"状态下停留 23 小时以上 —— 不通电的水泵，永远不会漏水。

────────────────────────────────────────

3. 硬件系统架构与拓扑 (Hardware Architecture)

系统在逻辑上由五大子系统构成：

3.0 三级断电架构 (Three-Layer Cutoff Topology)

水路供电的物理通路依次为：

  [市电 220V]
       ↓
  [L0 米家智能插座]    ← 仅在每日浇灌窗口（约 5–10 分钟）通电
       ↓
  [12V/4A 适配器]
       ↓
  [L1 软件干转互锁]   ← 水泵 PWM 仅在"至少一路阀已开"时允许输出
       ↓
  [L2 P-MOS 高边开关：5 路阀 Z1~Z5 + 水泵（AO4407A，高电平有效）]   ← 单次仅开 1 路阀
       ↓
  [地插喷头 → 花盆]

L0 由用户/米家定时驱动（物理总断电），L2 由本机 MCU 经 P-MOS 驱动；GPIO 复位为 Low 即全部 P-MOS 截止（失效安全）。L1 为软件互锁，保证泵不空转。L0 + 失效安全的 GPIO 默认态，使整套系统对"软件失控"仍具物理免疫（断电即不漏水）。

────────────────────────────────────────

3.1 供电分配网络 (PDN, Power Distribution Network)

采用"一源三支"的总线树状分流策略：

总源头：12V / 4A 直流稳压适配器（接 L0 米家插座下游）。

电源入口前级（V1.1 新增）：NTC 5D-9 浪涌抑制 + 470µF/25V 低 ESR 电解储能 + SMBJ15CA 双向 TVS 钳位，应对米家插座硬切上电瞬间的 di/dt 冲击。

动力网 (12V_Dirty)：直接引流，为水泵和水阀提供原始驱动能量；同时经 10kΩ 上拉为各路 AO4407A 的栅极提供截止偏置（GND_DIRTY 侧）。

纯净网 (5V_Iso & 3.3V)：经 B1205S-2WR3 隔离模块重塑，为 MCU、OLED、超声波模块提供毫无杂波的纯净电源。

> 注：V3.0 起取消机械继电器，**不再需要 5V_Dirty 线圈网（LM2596）**；脏侧仅保留 12V 动力网。

────────────────────────────────────────

3.2 大脑与交互子系统 (Control & HMI)

主控核心：STM32F401RCT6（Cortex-M4，84MHz，64KB SRAM，256KB Flash）。

选型理由（V1.1 补充）：
  · 国内电子市场现货价 F401RCT6 低于 F103C8T6（F401 为 ST 现役主推工艺，库存充足、单价占优）；
  · PWM 资源、定时器数量、Flash 末页 EEPROM emulation 均为本方案预留充足扩展空间；
  · 本方案功能未占满外设——选 F401 是"性价比 + 充裕预留"双优选择，非过度堆料。

人机界面：
  · **1.3 寸** OLED（128×64，SSD1306/SSD1315 兼容, I²C），显示浇灌路号、PWM 档位、错误码等。
  · **4 路独立按键模块**（按下输出低电平，MCU 内部上拉）：

按键定义：
  · K1  立即启动 / 跳过自检倒计时
  · K2  跳过当前路 / 下一项
  · K3  暂停 / 恢复（暂停时立即停泵并关闭全部水阀）
  · K4  进入参数菜单（每路时长、PWM 起止占空比、阶梯档数）

> 按键引脚：K1=PA6，K2=PA7，K3=PB0，K4=PB1（均内部上拉，按下为低电平）。

参数存储：使用外置 **AT24C08C**（I²C，1 KB）做配置持久化（双 Bank + CRC16），存储用户配置；保证米家插座断电后参数不丢失。

OLED 烧屏防护：每路浇灌期间动态翻转显示坐标，息屏 30s 后自动进入低对比度待机画面。

────────────────────────────────────────

3.3 动力与执行子系统 (Actuators)

主路动力源：12V 直流调速水泵，经 **AO4407A P-MOS 高边开关**供电，由 PA0 (TIM2_CH1) PWM 调速（当前 100 Hz）。

支路分配器：5 路 12V 常闭型电磁水阀（Z1~Z5），各路均由 **TLP281-4 光耦 + AO4407A P-MOS 高边**驱动（高电平有效）；水泵为第 6 路同构高边开关。无机械继电器。

终端：4× 地插式机械喷头（被动式，依赖出水压力决定射程半径）。

执行策略（V1.2 完整阐述）：

(a) 单路顺序执行
任意时刻仅有 1 路水阀吸合，避免水流分压、水锤叠加、压力骤降。第 i 路完成后再启动第 i+1 路。

(b) PWM 阶梯调速 → 喷射半径扫描
地插喷头是被动式机械喷嘴，**出水半径正相关于水泵出水压力**。本机以 PWM 占空比直接控制水泵转速，从而控制出水压力，进而控制喷射半径：

  低占空比（35%） → 低压、近距离淋洒（盆缘内圈）
  中占空比（55%） → 中压、覆盖盆中区
  中高占空比（75%） → 较高压、覆盖盆远端
  高占空比（95%） → 满压、最大射程（边缘冲洗）

默认 4 档（35% → 55% → 75% → 95%），每档停留时长 N 秒（菜单可调），由低到高逐步增大射程，使**单路 1 个喷头即可均匀覆盖整盆从近到远的全部半径**。这是本机执行策略的核心创新点：用调速代替多喷头机械分配。

(c) 路与路之间静默
切换路与路之间预留 500 ms 静默期，让水路压力卸荷，延长水阀寿命、避免水锤穿越下一路阀体。

────────────────────────────────────────

3.4 水位检测子系统 (Water Level Sensing)

3.4.1 选型对比

| 序 | 型号 | 类型 | 价位 | 优点 | 缺点 |
|---|---|---|---|---|---|
| ① | AJ-SR04M / JSN-SR04T | 防水超声波 | 25–40 元 | 连续测量、±1cm 精度、非接触、防水 | 顶部需 ≥25cm 空间；近端 <25cm 盲区 |
| ② | PSF-202 类磁浮球开关 | 干簧管浮球 | 5–15 元 | 无源、机械超可靠、可串入硬件回路 | 单点开关，无连续值 |
| ③ | XKC-Y25-V | 非接触电容式 | 30–50 元 | 贴水箱外壁，不开孔 | 需标定、壁厚敏感、易受手触干扰 |
| ④ | DFRobot LLC200A3SH | 光电折射式 | 30–60 元 | 寿命长、电极不接触水 | 单点；透明水箱失效 |
| ⑤ | 不锈钢双电极导通棒 | 电导式 | 3–8 元 | 极便宜 | 长期电解氧化、结垢、噪声大（不推荐） |

3.4.2 最终方案（① + ② 双保险）

主测：AJ-SR04M 防水超声波液位模块
  · 5V 供电，Trig/Echo 双线接入 STM32 GPIO；
  · 量程 25cm – 4.5m，精度 ±1cm；
  · 顶部贴装于水箱盖，软件每 1s 采样一次，OLED 实时显示"剩余 X%"；
  · 水位 < 20% → OLED 警示；水位 < 10% → 拒绝启动新一路浇灌。

兜底：PSF-202 类磁性浮球开关（干簧管 NC 常闭，Phase 2）
  · 安装于水箱底部 15% 高度；
  · V3.0 已取消机械继电器/线圈回路，**不再有可供浮球串联的线圈通路**；
  · 推荐接法：浮球干接点接入 MCU GPIO，水位过低时 MCU 立即停泵关阀（软件 Fail-Safe）；
  · 若需"不可被代码绕过"的硬件级兜底，可在 12V 动力网内单独串入浮球常闭触点，作为 Phase 2 选项单独评估。

水浸告警（可选项）：触发后由 MCU 立即停泵关阀并 OLED 红屏告警。

────────────────────────────────────────

3.5 硬件级防护子系统 (Hardware Protection)

详见 §4 核心护城河。

────────────────────────────────────────

4. 核心护城河与可靠性设计 (Critical Defenses)

4.1 纳秒级"反向水锤"吸收盾
针对水泵/电磁阀断电瞬间的上百伏反电动势，在每个感性负载端子处反向并联续流二极管：
  · 水泵：SS54 (5A/40V)（V1.1 由 1N5822 升级，应对堵转瞬态）；
  · 5 路水阀：1N5822 (3A/40V)，反向并联于各阀端子；
  · 水泵 MOS 漏极加 SMBJ24A TVS 二级钳位，作为续流二极管失效时的最后防线；
  · 因取消机械继电器，**不再需要触点 RC 灭弧网络**；续流二极管在 P-MOS 关断瞬间吸收阀/泵的反电动势。

4.2 强电侧"物理断点"与定制滤波
  · 物理沟槽：在 B1205S 隔离芯片底部刻断所有覆铜，强制拉开爬电距离；
  · π 型滤波：隔离模块输出端 10µF 高频去耦 + 10µH 大电流电感 + 47µF 低频储能；
  · 防飘高泄载：并联 470Ω 色环电阻作为常驻假负载，确保系统待机时电压精准锁定 5.0V；
  · 在 STM32F401 VDD 引脚最末端再加 0.1µF 高频陶瓷电容紧贴芯片焊盘。

4.3 水泵供电与干转互锁 (Fail-Safe & Dry-Run Interlock)
水泵 12V 经其 **AO4407A P-MOS 高边开关**供电，由 PA0 PWM 直接调速；无独立"总电源继电器"。

水泵 PWM 允许输出（duty>0）需满足：
  · MCU GPIO（PA0）有效驱动；
  · 至少一路电磁阀（Z1~Z5）已打开（`Bsp_Valve_AnyOn()` 软件干转互锁）；
  · Phase 2：水位安全（浮球/液位）。
任一条件不满足，固件拒绝置 PWM 并返回 `FM_ERR_013_PUMP_NO_POWER` —— 水泵不出力。

异常情形下 MCU 调用 `Bsp_Valve_ForceAllOff()` 并将 PWM 置 0；复位/掉电时所有 P-MOS 栅极被 10kΩ 上拉至 12V 而截止（失效安全）。

4.4 上电浪涌抑制 (Inrush Current Limiting)
米家插座硬上电瞬间，整机所有电容、隔离模块、负载同时充电启动，浪涌电流可达适配器额定值的 3–5 倍。三级抑制：
  (1) 电源入口 NTC 5D-9 限流（冷态 5Ω，热态降至 0.3Ω）；
  (2) 470µF/25V + 1µF 陶瓷的 LC 旁路；
  (3) SMBJ15CA TVS 钳位，截止电压 16.7V，应对感性反弹。

MCU 上电后强制延时 500ms 待电源稳定，再使能 P-MOS 高边与隔离侧负载（上电期间 GPIO 默认 Low，P-MOS 截止）。

4.5 看门狗与执行超时硬保护 (Watchdog & Hard Timeout)
  · 独立看门狗 IWDG：周期 256ms；超时复位后所有 P-MOS 默认截止（GPIO=Low）；
  · 执行超时三层硬限：
      - 单路浇灌时长上限 90 秒（菜单可调，绝对硬限 120 秒）
      - 路间切换静默 500ms 上限 2 秒
      - 单次任务总时长上限 8 分钟
  · 任一上限触发，立即停泵、关闭所有水阀，OLED 显示 ERR 码。

4.6 断电/失效安全架构（与 §3.0 呼应）
L0 米家插座（物理总断电）→ L1 软件干转互锁（泵须任一阀已开）→ L2 P-MOS 高边（GPIO=Low 即截止，复位/掉电失效安全）。各级独立、互为兜底。

────────────────────────────────────────

5. 软件状态机与任务流程 (Firmware State Machine)

系统以"一次性任务"为生命周期：

  [POWER_ON]
     │
     ▼
  [BOOT] ── 上电延时 500ms，初始化外设
     │
     ▼
  [SELFTEST] ── 读 EEPROM 配置；测水位；逐路阀短脉冲（50ms）通断自检
     │  (异常 → ERROR)
     ▼
  [IDLE_3s] ── OLED 倒计时 3 秒；K1 跳过；K3 取消
     │
     ▼
  [PUMPING_i (i=1..4)] ── 开阀 Z_i；水泵 PWM 阶梯（满足任一阀已开的干转互锁）
     │  水位低 / 超时 / 暂停按键 → SAFE_STOP
     ▼
  [INTER_GAP_500ms] ── 停泵（PWM→0）；关阀；卸压
     │
     ▼  (i < 4 → 下一路；i == 4 → 完成)
  [DONE] ── 停泵关阀；OLED 显示"任务完成"
     │
     ▼
  [SLEEP] ── MCU 进入 STOP 模式，等待米家插座断电

  [ERROR / SAFE_STOP] ── 立即停泵（PWM→0）+ 全部阀置 Low（P-MOS 截止）；
                         OLED 显示 ERR 码；
                         保持显示直到外部插座断电

特别说明：
  · 所有"停泵/关阀"动作均在 10ms 内完成；
  · 上电/复位默认全部 GPIO 为 Low（P-MOS 截止，水泵无电），符合 Fail-Safe；
  · 暂停（K3）状态下立即停泵关阀；
  · 任务结束后 MCU 进入 STOP 模式（< 10µA），但**不主动请求米家插座断电**，依赖米家定时窗口自然结束（简化外部协议）。

────────────────────────────────────────

6. 施工工艺与线束标准 (Wiring SOP)

线束如同血管，施工必须严苛：

6.1 AWG 标号规则（V1.2 勘误明确）
AWG 数字越大，线径越细。本方案选型经核：

| 规格 | 截面积 | 自由空气安全载流 | 在本方案的角色 | 余量 |
|---|---|---|---|---|
| 20 AWG | 0.518 mm² | ≈ 7.5 A | 主干 12V/4A 动力 + 地线 | ~87% |
| 24 AWG | 0.205 mm² | ≈ 3.5 A | STM32 排针级 5V/GND 信号馈线 | 充裕 |

20 AWG 主干为合理工程选择，无须升级 18 AWG。

6.2 大动脉（主干动力与地线）
UL1007 20AWG 红黑双色，主路 12V_Dirty 与 GND_Dirty 分别走线，禁止与信号线捆扎。

6.3 神经末梢（信号与纯净网变径处理）
对于需要接入 STM32 排针的 5V/GND 纯净网线缆，采用"20AWG 粗线 + 24AWG 杜邦母头"的绞合焊接变径工艺。焊点二次套热缩管包覆，消除杠杆撕裂应力。

6.4 双地绝缘
全系统严格遵循脏地（GND_Dirty）与净地（GND_Iso）分开汇集，严禁共地。两地仅通过 B1205S 隔离模块的初次级耦合存在能量传递路径。

6.5 水路施工
所有水管接口使用快插式 PE 管 + 内嵌 O 圈，禁止裸 PVC 软管 + 扎带；水箱出水口处加一道 200 目滤网，防止杂质卡阻水阀。

6.6 外壳
建议 ABS + IP54 等级，电气仓与水仓物理隔板，水仓底部预留导流槽，万一漏水可流出而非倒灌电气仓。

────────────────────────────────────────

7. 物料清单概要 (Bill of Materials Highlights)

| 子系统 | 关键物料 | 备注 |
|---|---|---|
| 外部托管 | 米家智能插座 2（Wi-Fi 版） | 用户自备/可选 Mesh 版 |
| 电源 | 12V/4A DC 适配器 + NTC 5D-9 + 470µF/25V + SMBJ15CA | 浪涌抑制三件套 |
| 电源 | B1205S-2WR3 隔离模块（5V_Iso） | 自行刻断+π 型滤波（已取消 LM2596 线圈网） |
| 主控 | STM32F401RCT6 + 1.3" OLED + 4× 独立按键 | 现货价优于 F103 |
| 存储 | AT24C08C（I²C EEPROM，1 KB） | 配置双 Bank + CRC16 |
| 驱动 | TLP281-4 光耦 ×N + AO4407A P-MOS ×6（5 阀 + 1 泵） | 高边开关，高电平有效；栅极 10kΩ 上拉 |
| 执行 | 12V 直流调速水泵 + 5× 12V 常闭电磁阀 + 地插喷头 | 水泵 PA0 PWM 调速 |
| 水位 | AJ-SR04M 超声波 + PSF-202 类浮球开关 | 双保险（详见 §3.4） |
| 保护 | SS54 ×1 + 1N5822 ×5 + SMBJ24A ×1 | 续流/钳位（无机械触点，免灭弧网络） |
| 线材 | UL1007 20AWG（主干） + 24AWG（末梢） | 红黑分色 |

────────────────────────────────────────

8. 关键技术指标 (Key Technical Targets)

| 类别 | 指标 | 目标值 |
|---|---|---|
| 电源 | 输入电压 | DC 12 V ±5% |
| 电源 | 输入电流（峰值） | ≤ 4 A |
| 电源 | 主干压降 | ≤ 0.1 V @ 4 A |
| 执行 | 水泵 PWM 频率 | 100 Hz（当前调速参数） |
| 执行 | 单路最长浇灌时长 | 默认 60 s，硬限 120 s |
| 执行 | 阶梯档数 | 默认 4 档（菜单可调 2–8） |
| 水位 | 超声波量程/精度 | 25 cm – 4.5 m，±1 cm |
| 水位 | 浮球响应时间 | ≤ 50 ms |
| 保护 | 异常停泵关阀响应时间 | ≤ 100 ms |
| 保护 | 看门狗周期 | 256 ms |
| HMI | OLED 分辨率 | 128 × 64 |
| HMI | 按键扫描周期 | 20 ms |
| 线束 | 主干 AWG | 20 AWG (0.52 mm²) |
| 线束 | 末梢 AWG | 24 AWG (0.21 mm²) |
| 寿命 | 整机设计寿命 | ≥ 5 年 |
| 寿命 | P-MOS 高边开关 | 无机械触点，循环次数不受限 |
| 待机 | 任务结束后整机功耗 | 由 L0 断电至 0（依赖米家窗口） |

────────────────────────────────────────

9. 后续演进 (Roadmap)

V1.2 → V1.3 候选改进：

(1) 引入土壤湿度传感器（电容式 4 通道），将开环定时升级为闭环条件触发；届时可考虑取消米家插座托管，改用自带 ESP32-C3 网关。

(2) ~~用 MOS 直驱替换机械继电器~~ —— **已在 V3.0 落地（AO4407A P-MOS 高边 + TLP281-4 光耦），降噪并消除触点寿命限制。**

(3) 浮球/液位低水位保护的硬件级兜底接法评估（见 §3.4）。

(4) 若确需掉电保留浇灌历史，改用更大容量外部存储（AT24C32/64 或 SPI Flash）后再规划日志环；**当前 AT24C08C 仅存配置，日志在 RAM。**

(5) 探索"多片地插喷头并联+单路 PWM 扫描"的更密植覆盖方案，将多路扩展为 N 个花盆。

────────────────────────────────────────
